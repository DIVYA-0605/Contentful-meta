import { KernelI } from '../../Kernel'


/**
 * SimpleBubble interface enlists all the props (and their types) that SimpleBubble can have.
 */
export interface SimpleBubbleI extends KernelI {
  //...enlist other
}